name: Deploy to DreamHost

on:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: deploy-dreamhost-admin
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build static site
        run: npm run build
        env:
          NEXT_PUBLIC_API_BASE_URL: ${{ vars.NEXT_PUBLIC_API_BASE_URL || 'https://balon-admin-api.spherearth.ca' }}

      # DreamHost SFTP → deploy into the SFTP user's home folder:
      #   /home/<DREAMHOST_SFTP_USERNAME>/
      # (Point balon-admin.spherearth.ca docroot at that user home in DreamHost.)
      # Secrets: DREAMHOST_SFTP_SERVER, DREAMHOST_SFTP_USERNAME, DREAMHOST_SFTP_PASSWORD
      - name: Install lftp
        run: sudo apt-get update && sudo apt-get install -y lftp

      - name: Sync site to DreamHost via SFTP
        env:
          SFTP_SERVER: ${{ secrets.DREAMHOST_SFTP_SERVER }}
          SFTP_USERNAME: ${{ secrets.DREAMHOST_SFTP_USERNAME }}
          SFTP_PASSWORD: ${{ secrets.DREAMHOST_SFTP_PASSWORD }}
        run: |
          if [ -z "$SFTP_USERNAME" ]; then
            echo "Missing secret DREAMHOST_SFTP_USERNAME"
            exit 1
          fi
          REMOTE="/home/${SFTP_USERNAME}/"
          echo "Deploying to ${REMOTE}"
          lftp -u "$SFTP_USERNAME","$SFTP_PASSWORD" "sftp://$SFTP_SERVER" <<EOF
          set sftp:connect-program "ssh -a -x -o StrictHostKeyChecking=no"
          mirror -R --delete --parallel=4 --verbose ./out/ $REMOTE
          quit
          EOF
